# Design 檔案審閱：paraacco 設計第 8 版 — 6 項決定逐條核對結果

**日期**: 2026-09-16
**審閱對象**: `Copy of paraacco 8.zip` 裡新增的
`paraacco copy-對齊後台copy.dc.html`（design 對 `DESIGN_TASK_paraacco-design-v7-scope-decisions_20260916.md`
6 項決定的回覆版本）
**核對方式**: 直接對新檔案的原始碼逐項 grep/比對，不是憑印象判斷；同時把 v7、v8 兩個 zip
裡除了這份新檔案以外的所有檔案做 byte-for-byte diff，確認沒有被意外改動。

---

## 先講結論

**6 項決定裡，5 項確認正確落實，1 項（信用卡）只做了表面裝飾，還沒做到決定要求的範圍。**
文件詳情頁分層這點（審閱文件第 2 節的建議）也確認做得很好，比我原本建議的做法更完整。

---

## 檔案異動範圍確認

先確認這次 design 只動了一個檔案，其餘都原封不動（逐一 byte-diff 過）：

| 檔案 | 狀態 |
|---|---|
| `paraacco copy-對齊後台copy.dc.html` | **新增**（v7 沒有這份） |
| `paraacco.dc.html` | 與 v7 完全一致，未變動 |
| `paraacco copy-加入專案預算系統.dc.html` | 與 v7 完全一致，未變動 |
| `財務管理.dc.html` / `.export.dc.html` | 與 v7 完全一致，未變動 |
| `信用卡帳單財務管理.html` | 檔案大小與 v7 相同（6,217,025 bytes） |
| `VaultLink.dc.html` | 與 v7 完全一致，未變動 |

## 決定 1：「客戶」維度拿掉 — ✅ 確認做到

在新檔案裡搜尋「範例客戶」「某某建設」「某品牌股份有限公司」「某市立高中」「客戶名稱」
等字串，**全部 0 筆**，客戶維度的顯示已經拿掉。

## 決定 2：專案編號格式改成 `AP_YYNNN` — ✅ 確認做到

舊格式「P-2026-011」**已無殘留（0 筆）**，新檔案裡統一使用 `AP_26003`／`AP_26008`／
`AP_26011`／`AP_26014` 四筆既有格式的範例編號，跟 `projects.id` 慣例一致。

## 決定 3：固定支出排程 — ✅ 確認做到，範圍符合「先緩後排、先畫示意版本」

新檔案裡保留了「固定支出」這個概念的**摘要示意畫面**（本月固定支出 47,890、YTD 累計
374,592 這類統計），但明確寫了引導文字：

> 固定支出以項目為主軸，每期帳單／發票仍各自建立購買案。點列切到「依購買案」，只看該分類的
> 所有帳單。

也就是沒有做成一個獨立的週期排程系統，而是做成一個「摘要視圖 → 點進去導回依購買案」的示意
畫面，符合決定 3「保留概念、先畫示意版本、不用現在就設計對應資料表」的範圍。

## 決定 4：信用卡部分 — ⚠️ 只做了表面裝飾，還沒做到決定要求的範圍

這項是唯一需要你留意的落差：

- 新檔案裡「信用卡」相關內容**只有一處**：某筆購買案明細上掛了一個寫死的顯示欄位
  「信用卡 末四碼 4821」，當作付款方式的標籤文字。
- **沒有**看到決定 4 要求的「信用卡主檔」畫面（銀行、卡別、末四碼、額度、結帳日、繳費日）,
  也沒有看到「連結對帳核銷」的操作介面（信用卡帳單如何連到既有的對帳/核銷機制）。
- 更重要的是，`財務管理.dc.html`／`.export.dc.html`／`信用卡帳單財務管理.html` 這三份
  「parallel・支出管理」品牌的檔案，決定 4 要求「只留信用卡主檔＋對帳核銷，其餘（年度總覽、
  家庭／工作室二分、一般商家統編主檔、支出分類系統）全部拿掉」——但這三份檔案**完全沒有被
  動過**（跟 v7 逐位元組相同），代表 design 端這輪還沒有處理這個決定，既沒有刪減也沒有重畫。

簡單說：「信用卡 末四碼 4821」只是在既有畫面上貼了一個文字標籤，不是決定 4 要的那個功能。
這也剛好對應 `DESIGN_TASK_paraacco-design-v7-scope-decisions_20260916.md` 裡提到的後台缺口
——現在 `statement_lines` 表完全沒有「這是哪張卡」的欄位，`credit_cards` 主檔表也還不存在
（這個缺口 Code 那邊還沒補，之前已經跟決定 3 一起排在後面，不用因為這次 design 有貼一個標籤
就誤以為功能已經做完）。

**建議**：這項先不用催 design 補完整的信用卡主檔畫面，維持「排在後面」的優先順序（跟決定 3
一樣）；但如果之後真的要排進工作範圍，記得畫面要重新設計，不能只靠這種寫死字串的示意方式，
且要等 Code 補好 `credit_cards` 表跟 `statement_lines`/`documents` 的關聯欄位，畫面才能真的
接得上資料。

## 決定 5：「依專案／依固定項目」瀏覽分頁拿掉 — ✅ 確認做到

搜尋「依專案」「依固定項目」**0 筆**；瀏覽分頁只剩「依購買案」（3 處）「依資產」「依文件」
三個，跟今天實際上線的「依標題瀏覽」入口範圍一致。

## 決定 6：`VaultLink.dc.html` 標記棄用 — ✅ 合規（不需要動作）

這份檔案本來就只需要「之後設計調整不用參考它」，不要求真的在檔案內加棄用標記，確認 v8
沒有改動這份檔案，符合預期。

## 額外確認：文件詳情頁分層（審閱文件第 2 節的建議）— ✅ 做得比建議的更完整

新檔案把文件詳情頁改成「文件／資產／時間軸／稽核」四個分頁，其中「稽核」分頁裡才放深度
除錯資訊，且再包一層「展開進階明細 ＋」／「收起進階明細 −」的收合控制，預設顯示「擷取結論
（已完成，欄位全數落庫）」「整體信心 96%」這種結論摘要。比原本建議的「收進一個展開區」
做得更細緻（用分頁 + 分頁內收合兩層處理），不需要再調整。

---

## 給你的下一步建議

1. **決定 1、2、3、5、6 跟文件詳情頁分層，這輪design端做得正確，可以直接採用，不用退回。**
2. **決定 4（信用卡）先不用催 design 補，維持排在後面的優先順序**——現在畫面上那個
   「信用卡 末四碼 4821」只是示意文字，不影響先前的排序決定，不用因為看到這個字串就以為
   功能已經做完或需要提前處理。
3. `財務管理`／`信用卡帳單財務管理` 這三份舊「parallel」品牌檔案目前完全沒被處理——如果
   之後要進行「只留信用卡主檔+對帳核銷」的裁剪，記得提醒 design 端這輪還沒動這三份檔案。

---

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01WKSyu8Aday4i627817y7My
