# Paraacco 財務文件自動分類/歸檔/勾稽 — 架構設計

文件版本：v0.1
日期：2026-09-12
定位：本文件是架構與介面設計，不含實作。實際 schema migration、Worker/Workflow 程式碼由本機 Code 執行，遵守專案「修正必進版、版本異動前必留存」原則。

前提對話：Theo 決定文件判讀不應由 Claude 逐份人工讀取，改為「掃描件丟給 Gemini API 判讀 → 回傳結構化標籤 → 自動歸檔」；憑證與對帳單的勾稽也由 Gemini 自動完成」；Claude 的角色收斂為整體架構設計與介面設計。

已拍板的三個方向：

- 執行環境——先分析利弊再建議（見第 1 節）。
- 勾稽範圍——憑證與對帳單自動勾稽。
- 儀表板——直接設計成 paraacco 的一部分（acco.parallelserver.org）。

對照基礎：`paraacco-v3-direction-review-20260912.md`、`paraacco-minimal-adjustment-analysis-20260912.md`（尤其是「調整 A：專案維度」的結論）、`paraacco-manual-execution-model-20260912.md`（人工執行模式的限制，本文件是它的後續）。

## 1. 執行環境：利弊分析與建議

### 選項 A：擴充 paraacco 既有 Cloudflare 後端

延用已驗證的 Gemini OCR pipeline（Queue → 8 步驟 Workflow → D1）、既有 purchases/assets/documents schema、Cloudflare Access 登入、acco-api.parallelserver.org 部署。

**優點：**

- Gemini OCR 已用真實掃描發票驗證過（DOC-2026-000002），不用重新驗證判讀能力本身。
- 資料庫、認證、檔案儲存（R2）都已存在，不用重建。
- 儀表板已拍板要放在 paraacco 底下——如果分類/歸檔資料放在別的地方，儀表板還是要跨系統讀資料，等於繞一圈做回同一件事。
- 跟既有的「調整 A：專案維度」分析完全對得上——這次要新增的「公司/專案/家庭個人」三分類，本質上就是把 ownership 維度（個人/公司/代墊/保管/移轉中）跟 project_id 外鍵接起來用，不是新概念。
- 長期只維護一套文件判讀 pipeline、一套供應商主檔、一套稽核軌跡。

**缺點：**

- apps/web 前端進度不明（Phase 1 收件匣、Phase 2 覆核工作台是否動工，還在等 Code 回報 `paraacco-status-confirmation-checklist-20260912.md`）。
- 09-04 的 Gemini provider 切換改動當時還沒 commit，現況也待確認。
- 目前 pipeline 的觸發方式是「使用者從網頁上傳」，掃描機資料夾的批次匯入是新的進件路徑，不管放在哪個平台都要新建。

### 選項 B：獨立輕量腳本/服務

**優點：**不受 paraacco 前端/部署現況影響，可以馬上動工；schema 比 paraacco 的採購/資產/文件三位一體單純很多。

**缺點：**既然儀表板已拍板要放進 paraacco，獨立服務的分類結果最終還是要匯入或同步到 paraacco 的資料層，等於多繞一手、多一套要對齊的 schema，還會重現「本地 Postgres 系統 vs paraacco 各自為政」在 `paraacco-minimal-adjustment-analysis` 第 0 節已經點名過的分工混亂問題。

### 建議：選項 A，但新開一條「本地批次進件」路徑，不等前端 Phase 1/2 完工

理由：儀表板拍板放進 paraacco，就已經實質上決定了資料層也該在 paraacco——選項 B 只是把選項 A 的整合成本延後，不是省掉。但也不需要等 apps/web 的網頁上傳 UI（Inbox）做完才能開始，因為掃描機資料夾的批次匯入本來就不是「使用者從網頁拖檔案」的流程，可以直接用 API 呼叫把檔案送進既有的 `paraacco-document-ingest` Queue，跳過網頁上傳表單這一層。也就是說：

- 判讀/分類/儲存的後端（Queue、Workflow、Gemini OCR、D1）現在就能接，不受前端進度影響。
- 前端（Inbox 網頁上傳畫面）跟這次的批次匯入需求無關，維持照原路線圖走。
- 唯一的前置條件：確認 A2（commit 現況）沒有卡住 Queue/Workflow 能不能正常收件——這件事本來就在等 Code 回報，屬於既有待辦，不是這次新增的阻塞點。

## 2. 整體資料流

```
[使用者裝置] 掃描機資料夾 (Bookkeeper_Scanner)
      │  每週排程觸發（本機 device_bash，見第 4 節）
      ▼
[上傳] 新檔案 → paraacco 既有進件 API → R2 存檔 → paraacco-document-ingest Queue
      ▼
[Gemini 判讀 Workflow]（沿用既有 8 步驟，擴充輸出欄位，見第 3 節）
  → 判定 ownership scope（公司/專案/家庭個人/待覆核）
  → 判定 entity（哪個法律主體，見第 3.2 節）
  → 抽取日期/對象/金額/文件類型
  → 信心分數低 or 供應商未登記 → 進 review 佇列（沿用既有強制覆核規則）
      ▼
[對帳類文件] 走同一條進件路徑，Gemini 額外抽取「對帳單明細列」（日期/金額/摘要）
      ▼
[勾稽 Workflow]（新增，見第 5 節）
  → 明細列 × 憑證記錄 自動比對 → 已勾稽/待覆核/金額不符
      ▼
[本機歸檔收尾]（見第 4 節）
  → 上傳確認成功 → 原始檔從 Bookkeeper_Scanner 移到本機「已上傳」資料夾
  → Bookkeeper_Scanner 每週清空（達成 Theo 原始要求）
      ▼
[儀表板]（acco.parallelserver.org 新頁面，見第 6 節）
  → 依 公司/專案/家庭個人 分類總覽
  → 對帳狀態總覽
  → 待覆核佇列
```

判讀、分類、比對三件事全部發生在 Gemini API 呼叫裡（透過 paraacco 既有的 Workflow 框架去呼叫），Claude 不參與逐份判讀。

## 3. 分類 schema 設計（延用「調整 A」，不做 v3 全套重寫）

### 3.1 沿用既有維度

- ownership（個人/公司/代墊/保管/移轉中）——維持不變，家庭個人對應「個人」，公司對應「公司」。
- 新增 `projects` 表 + `purchases.project_id` 外鍵（「調整 A」原案），取代目前 `sub` 欄位的鬆散文字，用來承載「專案」這一類。

### 3.2 新增：entity 維度（這次新發現的缺口）

批次判讀過程中發現至少 3 個不同法律主體目前都被歸在同一個「公司」標籤下：平行空間有限公司（統編 83018456）、呂劭翊建築師事務所（統編 158464165，獨資）、平行空間室內裝修有限公司（借據上出現）。建議新增一張輕量 `entities` 表（id/name/tax_id/type），`purchases` 加一個可空的 `entity_id` 外鍵——跟 ownership=corp 是正交的兩個維度（ownership 決定「這是公司的還是個人的」，entity 決定「是哪一個法律主體的」），不影響既有的 ownership 狀態機。

> 此節「3 個法人」的判斷已於第 13.2 節修正為 2 個，以第 13.2 節為準。

### 3.3 跨界標籤

沿用批次試跑時建立的「CORP-PERS」（公司/個人跨界，附註待確認）慣例，對應到 ownership=advance（公司代墊）或新增一個 `pending_scope` 布林欄位，讓這類文件自動進 review 佇列，不強迫 Gemini 一次判斷到底。

### 3.4 Gemini 判讀 Workflow 的輸出欄位（在既有欄位上擴充）

| 欄位 | 來源 | 說明 |
|---|---|---|
| ownership | 既有 | 公司/個人/代墊/保管/移轉中 |
| entity_id | 新增 | 對應 3.2 |
| project_id | 新增（調整 A） | 對應 3.1，Gemini 判斷不出來時留空，進 review |
| doc_type | 既有 acct 欄位精神延伸 | 發票/收據/繳費證明/estimate/合約… |
| vendor / payee | 既有 | 供應商比對邏輯不變 |
| amount / date | 既有 | — |
| confidence | 既有 | 低信心或供應商未登記 → 強制 review（既有規則，不變） |
| notes | 新增 | Gemini 產生的一句話備註（例如「手寫金額不清楚」），對應批次試跑時人工寫的備註欄 |

## 4. 本地掃描資料夾銜接（每週排程）

用 Claude Code Remote 的排程工具（`create_trigger`，`requires_local_device: true`）建立每週一次的排程，在使用者裝置上執行，而不是本 session 手動觸發：

1. `device_bash` 掃描 Bookkeeper_Scanner，找出新檔案（跟上次執行的紀錄比對，或單純處理資料夾裡現存的所有檔案）。
2. 排除已知非財務位置——Atelier_Scanner、Maison_Scanner 不在掃描範圍內（Theo 已確認）。
3. 用 HTTPS 呼叫 paraacco 的既有上傳 API，把檔案連同 `source: local-scanner-batch` 標記送進 R2/Queue。
4. 上傳確認成功後，把本機原始檔從 Bookkeeper_Scanner 移到新建的「已上傳」資料夾（不是刪除，保留原始檔案，符合專案「版本異動前必留存」的精神）。

Bookkeeper_Scanner 因此每週自動清空——達成 Theo 最初的要求，但現在「清空」的觸發條件是「已成功上傳進 paraacco」，不是「已被人工判讀完」。

這一步只搬檔案、呼叫 API，不做任何判讀邏輯，所以不受「Claude 逐份讀取」問題影響，也不需要本 session 常駐。

> 第 13.3 節補充：實際排程需要同時處理兩個來源位置（USB 掛載的 Scanner + SMB 掛載的 NAS 對帳分享），見該節。

## 5. 憑證 × 對帳單自動勾稽

新增 `statement_lines` 表（entity_id/date/amount/description/source_document_id），對帳類文件（信用卡/銀行對帳單）走同一條 Gemini OCR pipeline，但輸出的是「明細列陣列」而不是單筆金額。

比對邏輯（新增一個 Workflow 或排程 job）：

- **第一輪**：金額完全相符 + 日期在容許區間（例如 ±3 天）+ 供應商名稱相似度高 → 自動標記「已勾稽」。
- **第二輪**：金額相符但日期/供應商對不上，或供應商名稱模糊 → 交給 Gemini 做語意比對輔助判斷（例如判斷「合興汽車保養廠」跟對帳單上的「合興車業」是不是同一家），標記「建議勾稽，待確認」進 review 佇列。
- **都對不上的** → 標記「未勾稽」，可能是現金交易或對帳單还没进来。

這塊是全新功能，paraacco 目前完全沒有這個機制，需要新的 schema 跟新的 Workflow 步驟，工程量比純分類大，但符合 Theo 這次明確要的「自動勾稽」範圍。

## 6. 儀表板設計（acco.parallelserver.org 新增頁面）

延續既有 vaultlink 五階段 UI 路線圖（Inbox → Review → List → Dashboard → Reports），在其中插入本次需要的畫面，不是另起一個系統：

- **總覽（Dashboard 擴充）**：依 公司 / 專案 / 家庭個人 三個分頁切換，每個分頁顯示本月金額、待覆核筆數、勾稽狀態摘要。
- **List 畫面擴充篩選**：新增 entity、project、勾稽狀態 篩選條件。
- **Review 工作台擴充**：低信心或跨界（CORP-PERS）文件在既有覆核流程裡多顯示「建議 entity/project」讓人一鍵確認，而不是重新設計一套覆核介面。
- **對帳頁（新增）**：列出 statement_lines，標示已勾稽/待確認/未勾稽，點進去可看對應的憑證文件。

## 7. 與既有決策的關係

這個設計不是 v3 四入口模型的重寫，而是 `paraacco-minimal-adjustment-analysis` 裡「調整 A（專案維度）」的具體落地，加上一個原本沒分析到的「entity 維度」缺口，加上一個全新但範圍明確的「對帳勾稽」功能。不涉及雙式分錄、不涉及月結鎖帳、不推翻 09-02 「個人帳本不併入 paraacco」的決策的實質——因為這裡的「家庭個人」分類只是 ownership=個人 這個既有狀態，不是把整個家庭記帳系統做進 paraacco。

## 8. 待確認/銜接事項

- `paraacco-status-confirmation-checklist-20260912.md` 的回報結果——A2 commit 現況會直接影響第 1 節「選項 A」能不能馬上動工。（**已於第 11 節解決**）
- 對帳類文件（信用卡/銀行對帳單）目前散落的實際位置，還沒有具體路徑——決定第 4 節排程要掃描哪個資料夾。（**已於第 13.3 節拍板**）
- entities 表的主體清單需要 Theo 確認完整名單（目前已知 3 個，可能還有遺漏）。（**已於第 13.2 節拍板，修正為 2 個**）
- 批次試跑中標記「待確認」的 4 筆文件（合興汽車保養廠估價單、台北自來水費、林建成建築五金估價單、馬祖小客車租賃）——這些是新 schema 上線後第一批要跑過 review 佇列的真實案例，可以直接當驗收測資。

## 9. 下一步

這份文件可以交給本機 Code，作為新一輪工程規劃的輸入（比照 `paraacco-status-confirmation-checklist` 的交接模式）。實際的 migration、Workflow 程式碼、排程腳本由 Code 執行並遵守版本留存原則；本 session 之後的角色維持在架構調整與介面設計，不參與逐份文件判讀。

## 10. 追加決策（2026-09-12）：本地 Postgres/Directus/n8n 方案全部放棄

Theo 確認本地那套「個人/工作室財務系統」（PostgreSQL + Directus + n8n on DS923）整套放棄，不是延後。這解除了第 7 節跟本文件討論過程中一直懸著的架構矛盾——paraacco 原始藍圖裡「延伸個人財務架構」的定位不再適用，因為被延伸的對象已經不存在。

影響：

- 家庭/個人範圍的對帳勾稽（第 5 節)不再是「暫時借用」，直接是永久方案——不需要再保留「之後可能要搬家」的但書，第 5 節的 `statement_lines` 設計與比對邏輯可以直接視為最終架構，不用切成「公司先做、家庭延後」兩階段。
- DS923 上原本規劃給那套系統的 Docker/Postgres/Directus/n8n 環境建置工作，全部取消，不需要再排入任何路線圖。
- `household_transactions` / `household_documents` 那套 schema 設計作廢——第 3 節的 ownership=個人 + entity/project_id 維度是現在唯一的家庭/個人資料模型，不需要再跟一套假想中的 Postgres schema 對齊或預留遷移空間。
- 第 1 節「選項 A」的建議理由更加確立：既然不會再有第二套系統來分工，paraacco 統一處理公司/專案/家庭個人三個範圍，是唯一選項，不是眾多選項裡權衡出來比較好的一個。

第 8 節「待確認事項」中「本地 Postgres 計畫死活」這一項現在已經解決，可以從待辦清單移除；其餘三項（Code 現況回報、對帳類文件實際位置、entities 主體清單）維持待確認。

## 11. 追加（2026-09-13）：Code 現況回報結果——地基疑慮解除，但有 3 件事建議先處理再動工

Code 回報 `paraacco-status-confirmation-checklist-20260912.md` 的結果，第 1 節「選項 A」原本擔心的地基風險（A2 commit 現況、apps/web 前端進度）已經解除：

- **A2（commit 現況）**：已完成。Gemini provider 切換已 commit，拆成邏輯完整的多筆 commit，unpdf 等實驗性殘留也已清乾淨，working tree 目前乾淨。
- **A3（apps/web 部署）**：已完成。acco.parallelserver.org／acco-api.parallelserver.org 兩個網域都正常，Workers + OpenNext 部署確認，API_BASE 已指向正確網址。
- **前端 UI 進度**：不只 Phase 1-2，B 類 Phase 1-5 全部做完（收件匣、待覆核、文件列表/詳情、Dashboard、報表/管理/搜尋），還有一輪額外補完（資產編輯、通知中心、範圍切換器、JWT 驗證等）。
- **Cloudflare 資源**：Queue、INTERNAL_SERVICE_TOKEN、D1 migration（已演進到 47 個 CHECK 約束）都確認存在且結構正確；GitHub repo 已確認 public。

結論：第 1 節「選項 A」的建議不再是「假設地基沒問題」的有條件推薦，地基已經確認穩固，可以直接照這份架構文件的設計往下做 schema 擴充（entity/project/statement_lines）與第 4 節的批次進件開發。

但回報裡也點出 3 件事，建議在正式啟動第 4 節「每週自動排程進件」之前先處理掉，避免新的自動化流程疊加在還沒驗證過的地基上：

1. **分支現況需要 Theo 決定**：本機 main 實際 push 到的是 origin/dev，origin/main 本身還停在很早期（落後、沒有 Gemini 切換之後的任何 commit）；另外 origin/wip/golive-a2-a3-b-20260904 是可以清掉的舊分支。建議先決定要不要把 origin/main fast-forward 到 dev，把分支現況理清楚，再開始加新的 migration，不然之後 entity/project/statement_lines 這些改動會不知道該疊在哪個分支上。（**已於第 13.1 節拍板**）
2. **端到端驗收步驟 5、6 需要 Theo 本人操作**：真實網頁上傳路徑（不是模擬 API 呼叫）、多類型單據（電子發票 PDF／手機拍照 jpg／非發票類單據）批次驗證，都卡在需要登入 Cloudflare Access 才能做。建議在把「掃描機資料夾週排程」接上既有 pipeline 之前，先手動跑過這兩步，確認 pipeline 本身在真實使用情境下沒問題，不要讓自動化流程變成第一個真正測試這條路徑的東西。另外 DOC-2026-000009 先前狀態是 failed，需要確認是否已解決，避免新流程遇到同一個問題卻沒人發現。（**維持待辦，見第 13.5 節**）
3. **vendors 整合（paraacco vs paraarch）仍未決定**：不影響這次的 entity/project/勾稽設計，可以先擱置，不是阻塞項。

第 8 節待確認清單更新為：（1）分支 fast-forward 決定、（2）端到端驗收步驟 5/6 + DOC-2026-000009 確認、（3）對帳類文件實際存放位置、（4）entities 主體清單。「Code 現況回報」本身已完成，從清單移除。

## 12. 待處理事項詳細清單與建議做法（2026-09-13）

### 12.1 分支現況(origin/main 落後、wip 舊分支)

**風險**：如果之後 entity/project/statement_lines 的新 migration 不知道該接在哪個分支，容易造成「正式跑的是哪個版本」搞不清楚，尤其現在 main／dev 已經分岔。

**建議做法**：

1. 先確認 Cloudflare 的部署管線實際是接哪個分支觸發（多半是 dev，因為 A3 確認的部署內容包含 Gemini 切換之後的東西）。
2. 如果確認是 dev：把 origin/main fast-forward 到跟 dev 一致（`git push origin dev:main`），讓 main 重新變成名實相符的主分支；之後新工程一律先進 dev，穩定後再同步回 main，跟現有習慣一致，不用改工作流程。
3. 刪掉 origin/wip/golive-a2-a3-b-20260904（已落後 dev 9 個 commit，內容已被取代）。

**執行者**：交給 Code 動手（git 操作、確認部署設定），但「要不要 fast-forward main」這個決定本身由你拍板一次即可，之後不用每次都問。

**順序**：建議排在開始寫 entity/project/statement_lines migration 之前，避免新 schema 改動疊在分支狀態不明確的地基上。

> **已於第 13.1 節拍板為：先備份 → 直接覆蓋 main。**

### 12.2 端到端驗收步驟 5、6 ＋ DOC-2026-000009

**風險**：目前資料庫裡的紀錄都是模擬 API 呼叫或反覆用同一份發票測試，真正的「網頁拖放上傳」路徑跟「電子發票／手機拍照／非發票類單據」三種來源混合的情況都還沒被驗證過。如果直接把每週排程自動化接上去，等於讓自動化流程第一次真正測試這條路徑，出問題時不容易分辨是新功能的問題還是既有 pipeline 本來就有的問題。

**建議做法**：

1. 你自己登入 acco.parallelserver.org，用真實拖放上傳做一次測試（不是叫 Code 模擬 API 呼叫），確認能正常走完 Queue → Workflow → 落地。
2. 準備 3 種來源類型各一份做批次測試：電子發票 PDF、手機拍照 jpg、非發票類單據（估價單/收據都可以）——手邊現成材料就是這次批次試跑裡標記「待確認」的幾份文件（合興汽車保養廠估價單字跡不清、林建成建築五金估價單同樣手寫不清），剛好可以當這輪驗收的邊界案例測資，不用額外找新文件。
3. 順便查一下 DOC-2026-000009 當時失敗的原因是不是已經解決（可以請 Code 查 D1 紀錄跟錯誤日誌），避免同樣的 bug 之後被自動化流程重複踩到卻沒人發現。

**執行者**：你本人做上傳測試（需要 Cloudflare Access 登入態，Code 沒辦法代勞）；DOC-2026-000009 的原因調查可以交給 Code。

**順序**：建議在啟動第 4 節「每週自動排程進件」之前做完，屬於開關自動化前的最後一道確認，不影響 entity/project 這些 schema 設計本身現在就能開始。

### 12.3 對帳類文件（信用卡／銀行對帳單）實際存放位置

**風險**：沒有固定來源資料夾，第 5 節的自動勾稽功能設計得再完整也沒有東西可以自動抓取，這件事技術上解決不了，純粹是你的使用習慣問題。

**建議做法**：

1. 先想清楚這些對帳單目前實際上怎麼進到你手上——是每月自己上網銀行網站下載 PDF、還是銀行寄 email 附件、還是信用卡公司寄紙本再掃描？三種來源的自動化難度差很多。
2. 如果是網銀下載：比照 Bookkeeper_Scanner 的模式，固定存到一個資料夾（例如同一顆 Scanner 硬碟底下開一個「對帳類」資料夾），排程掃描這個資料夾即可，做法跟現有設計完全一致。
3. 如果是 email 附件：studio 本來就有 paramail 共用信箱系統，可以考慮設一條規則把對帳單附件自動存到指定資料夾，再用同一套排程掃描——但這是額外一塊小工程，不是現在這份架構文件範圍內。
4. 如果三種來源都有、沒辦法統一：至少先固定一種當作「標準路徑」，其他來源手動搬過去，不需要一次做到完全自動化。

**執行者**：完全是你的決定，我沒有資訊可以幫你判斷實際狀況；只要你給一個具體資料夾路徑，第 4 節的排程設計可以直接套用。

> **已於第 13.3 節拍板：`smb://192.168.20.91/ATLPAR_Bookkeeper/Paraacco_公司財務系統`。**

### 12.4 entities 主體清單

**風險**：清單不完整，之後補登新法人時舊文件可能要重新歸戶一次。

**建議做法**：

1. 目前已知 3 個：平行空間有限公司（統編 83018456）、呂劭翊建築師事務所（統編 158464165，獨資）、平行空間室內裝修有限公司（僅在借據上出現，統編未知）。
2. 你直接確認這 3 個是否為全部，或還有其他登記中/曾經登記過的法人（例如其他家族事業、其他獨資登記）。
3. 針對「平行空間室內裝修有限公司」補一下統編跟目前是否仍在使用中的狀態，因為目前只有借據這一份文件提到它。
4. 這件事很輕量，不需要額外分析，直接在對話裡回答我就可以建表。

> **已於第 13.2 節拍板：修正為 2 個法人（平行空間室內裝修有限公司是別名，非獨立法人）。**

### 建議處理順序

1. entities 清單（12.4）——你現在就可以直接回答，零成本。
2. 分支決定（12.1）——一次性拍板，交給 Code 執行。
3. 對帳類文件位置（12.3）——需要你想清楚實際習慣，不急但會卡住第 5 節功能。
4. 端到端驗收（12.2）——排在正式打開每週自動排程開關之前做，不卡現在的 schema 設計工作。

## 13. 追加決定（2026-09-13）：12.1、12.3、12.4 已拍板，12.2 維持待辦

### 13.1 分支處理方式（回應 12.1）——已拍板

做法：先把現有 repo 存檔到本地，再直接用 main 覆蓋——也就是先建立 origin/main 目前狀態的本地備份/快照（符合專案「版本異動前必留存」原則），確認備份完成後，把 main 直接覆蓋成跟 dev 一致的內容（例如備份後 `git push origin dev:main --force` 或本機 reset 再 push），之後統一用 main 當唯一分支往下做。舊的 `origin/wip/golive-a2-a3-b-20260904` 一併清掉。這件事交給 Code 執行，執行前的本地備份步驟不可省略。

### 13.2 entities 主體清單（回應 12.4）——已拍板，從 3 個修正為 2 個

原本判斷的第三個法人「平行空間室內裝修有限公司」（僅在借據上出現）確認是平行空間有限公司的別名，不是獨立法人。最終 entities 清單：

| entity_id | 名稱 | 統編 |
|---|---|---|
| ap | 平行空間有限公司（含文件上出現「平行空間室內裝修有限公司」字樣的情形） | 83018456 |
| studio | 呂劭翊建築師事務所 | 60277434(2026-09-13 晚間修正,原誤植為 158464165) |

第 3.2 節、第 10 節、第 12.4 節提到「3 個法人」的敘述以本節為準修正。之前批次試跑的借據（`20260903_CORP-PERS_LOAN_呂瑞雍借款_200000.pdf`）不需要重新歸戶，只是文件上的法人名稱要理解成同一個 entity。

### 13.3 對帳類文件存放位置（回應 12.3）——已拍板

確認路徑：`smb://192.168.20.91/ATLPAR_Bookkeeper/Paraacco_公司財務系統`（NAS 分享，跟憑證類使用的 Scanner USB 路徑是不同的網路位置）。

影響第 4 節的每週排程設計：排程需要接觸兩個來源位置，不是一個——

- 憑證類：本機掛載的 Scanner USB（Bookkeeper_Scanner）。
- 對帳類：NAS SMB 分享（`ATLPAR_Bookkeeper/Paraacco_公司財務系統`）。

兩者都走同一套「上傳確認成功後搬到已上傳歸檔位置」的邏輯，差別只在來源掛載方式不同（USB vs SMB），排程腳本需要能同時處理兩種掛載點。

### 13.4 Gemini 歸檔命名規則——已產出

Gemini 判讀後要套用的分類/命名規則已經寫成獨立文件 `paraacco-gemini-filing-naming-rules-20260913.md`，內容涵蓋範圍碼、類型碼、對象、金額的組成規則，以及對應的 JSON 輸出 schema，可以直接交給 Code embed 進現有 Workflow 的 Gemini 呼叫步驟。文件裡特別澄清一點：這套命名規則落地時是 documents 表的 `display_name` 欄位，不是去重新命名/搬移 R2 裡的實體檔案——因為 schema v2 本來就刻意讓 R2 key 脫鉤業務關聯，維持這個設計不變。

### 13.5 待辦更新

第 8 節／第 12 節待確認清單，扣除本次已拍板的 12.1、12.3、12.4，只剩 **12.2（端到端驗收步驟 5/6 ＋ DOC-2026-000009）** 需要你本人操作，其餘都可以交給 Code 開始動工：分支處理（13.1）→ schema migration（entities/projects/statement_lines，套用 13.2 的 2 個 entity）→ 把命名規則文件（13.4）embed 進 Gemini 呼叫 → 接上兩個來源位置的每週排程（13.3）。
