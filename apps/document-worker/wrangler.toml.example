# 複製成 wrangler.toml 後依實際 Cloudflare 資源 ID 填入。wrangler.toml 本身不進版控。

name = "paraacco-document-worker"
main = "src/index.ts"
compatibility_date = "2026-09-01"

[[r2_buckets]]
binding = "FILES"
bucket_name = "paraacco-files"

# 是否需要 Cloudflare Queues 排 OCR 工作,待評估(見根目錄 README)。
